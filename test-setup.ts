// Minimal test setup placeholder required by package vitest configs in this repository.
// Intentionally minimal: no-op. Created to satisfy test runner when running isolated tests.

export default undefined;
